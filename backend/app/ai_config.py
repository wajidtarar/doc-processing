# Centralized model names — pinned to stable (non-preview, non-"-latest") versions
# on purpose, so behavior stays reproducible while we're learning. Swap to
# "-latest" aliases later if you want to always track Google's newest release.
MODEL_FLASH_LITE = "gemini-3.1-flash-lite"
MODEL_FLASH = "gemini-3-flash-preview"  # confirmed working in Phase 0